import "mem.gg";

class Vec {

    ptr data;
    u64 size;
    u64 capacity;

    Vec() {
        this.size = 0;
        this.capacity = 8;
        this.data = malloc(capacity);
    }

    ~Vec(){
        free(this.data);
    }

}