// Compute factorial recursively and use it in a for-loop sum.
// Returns the result as the process exit code so it is observable
// without any I/O support in the language yet.
//
//   factorial(5) = 120
//   sum 1..5     =  15
//   result       = 120 - 15 = 105

fn factorial(i32 n) -> i32 {
    if (n <= 1) { return 1; }
    return n * factorial(n - 1);
}

fn sumTo(i32 n) -> i32 {
    mut i32 total = 0;
    for (mut i32 i = 1; i <= n; i++) {
        total = total + i;
    }
    return total;
}

fn main() -> i32 {
    i32 fact = factorial(5);   // 120
    i32 sum  = sumTo(5);       // 15
    return fact - sum;         // 105
}
