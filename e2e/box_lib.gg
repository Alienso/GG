// A small generic "library" used by cross_file_generics_test.gg.
class Box<T> {
    T value;
    Box(T v) { this.value = v; }
    T get()  { return this.value; }
}

T maxT<T>(T a, T b) { if (a > b) { return a; } return b; }
